import 'dart:convert';

import "package:http/http.dart" as http;

class Service {
  //generic class
  var PROTOCOL = "http";
var DOMAIN = "easy-tution.herokuapp.com";
  void getAll(DOMAIN) {
    
  }
  void getOne(DOMAIN) {
    
  }
  void create(DOMAIN) {
    
  }
  void update(DOMAIN) {
    
  }
  void delete(DOMAIN) {
    
  }

}
